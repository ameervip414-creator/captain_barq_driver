name: captain_barq_driver
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase - نسخة موحدة ومستقرة
  firebase_core: ^3.6.0
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.6.12
  firebase_database: ^11.1.5

  # الخرائط والموقع
  google_maps_flutter: ^2.18.0
  geolocator: ^14.0.3
  permission_handler: ^11.4.0
  flutter_local_notifications: ^19.2.1

  # باكجات صفحة الارباح
  fl_chart: ^0.68.0
  intl: ^0.19.0
  workmanager: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true